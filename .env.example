# LLM Configuration
# Set your preferred LLM provider: openai or mock
LLM_PROVIDER=mock

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Mock LLM (for testing without API keys)
# No configuration needed - mock responses are generated automatically
